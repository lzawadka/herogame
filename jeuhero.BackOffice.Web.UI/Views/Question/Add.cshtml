@model jeuhero.Core.Data.Models.Question

@{
	ViewData["Title"] = "Add";

	List<jeuhero.Core.Data.Models.Paragraphe> list = null;

	list = ViewBag.ParagrapheList;

	var listItems = list.Select(item => new SelectListItem() { Text = item.Title, Value = item.Id.ToString() }).ToList();
}

<h1>Add</h1>

<h4>Question</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Add">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <select asp-for="ParagrapheId" asp-items="listItems"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
